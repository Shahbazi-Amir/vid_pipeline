name: Ocean Aram source probe
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/ocean-aram-probe.yml'
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'Ocean Aram source probe'
