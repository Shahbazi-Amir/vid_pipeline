name: Stop private release collections

on:
  push:
    paths:
      - ".github/workflows/stop-private-collections.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: private-release-collections
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - name: Stop active private collection processing
        run: echo "External-review spending stop requested; cancel any active private collection run in this concurrency group."
