# Standalone architecture

The repository accepts a video URL, stores all runtime files under `outputs/<job-id>`, and returns machine-cleaned Markdown and plain-text transcripts. It does not publish to or depend on another repository.
